# trmnl-agent-dashboard
A lightweight status service that lets OpenClaw agents publish high-level progress updates to a secure endpoint on a schedule, then exposes a clean read-only summary for TRMNL to display.
